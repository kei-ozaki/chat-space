.header
  .header__left
    .chat__name
      = @group.name
    .chat__member
      MEMBER:
      / = @group.users.each do |user|
      /   = user.name
  .header__right
    = link_to edit_group_path(current_user), class: 'chat-edit' do
      Edit
