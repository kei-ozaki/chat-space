!!!
%html
  %body
    .chat-side
      .header
        .name
          = current_user.name
        .ul.icons
          .li.list
            = link_to new_group_path do
              = fa_icon 'edit', class: 'icon'
          .li.list
            = link_to edit_user_path(current_user) do
              = fa_icon 'cog', class: 'icon'

      .groups
        - current_user.groups.each do |group|
          .group
            = link_to group_messages_path(group) do
              .group__name
                = group.name
              .group__message
                メッセージはありません。

    .chat-main
      .chat-main__header
        .chat-main__header__left
          .chat__name sample group
          .chat__member Member: taro

        .chat-main__header__right
          .chat-edit Edit

      .chat-main__contents

        .ul.message
          .li.user__name taro
          .li.date その時間
          .text 本文

        .ul.message
          .li.user__name taro
          .li.date その時間
          .text 本文

      .chat-main__footer
        .footer__input
          .input__message type a message
          .fa.fa-picture-o
        .footer__send Send
